"use strict";
const logDetails = (uid, item) => {
    console.log(`${item} has a uid of ${uid}`);
};
const greets = (user) => {
    console.log(`${user.name} says hello`);
};
const greetU = (user) => {
    console.log(`${user.name} says hello`);
};
// so we then write the function like this
const loggedDetails = (uid, item) => {
    console.log(`${item} has a uid of ${uid}`);
};
const greeting = (user) => {
    console.log(`${user.name} says hello`);
};
